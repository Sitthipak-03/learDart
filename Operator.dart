void main() {
  // ข้อมูลสมัครงาน
  String gender = "ชาย";
  double age = 20;
  // bool cond1 = (gender == "ชาย");
  // bool cond2 = (age == 20);
  // bool result = cond1 && cond2;
  // print(cond1);
  // print(cond2);
  // print(result);
  if (gender == "ชาย" && age == 20) {
    print("ผ่านการสมัครงาน");
  } else {
    print("ไม่ผ่าน");
  }
}
